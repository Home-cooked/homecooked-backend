# Elixir CircleCI 2.0 configuration file
#
# Check https://circleci.com/docs/2.0/language-elixir/ for more details
version: 2.1
jobs:
  build:
    docker:
      # specify the version here
      - image: circleci/elixir:1.9

      # Specify service dependencies here if necessary
      # CircleCI maintains a library of pre-built images
      # documented at https://circleci.com/docs/2.0/circleci-images/
      # - image: circleci/postgres:9.4

    working_directory: ~/repo
    steps:
      - add_ssh_keys:
          fingerprints:
            - "ae:39:28:e5:90:76:56:f1:9b:46:34:8c:ed:85:ba:8f"

      - checkout

      # specify any bash command here prefixed with `run: `
      - run: mix local.hex --force
      - run: mix local.rebar --force
      - run: mix deps.get
      - run: mix deps.compile

      - run: echo "$CIRCLE_SHA1"
      - run: ssh -o StrictHostKeyChecking=no admin@$EC2_DOMAIN_NAME "mkdir homecooked-$CIRCLE_SHA1"
      - run: scp -r * admin@$EC2_DOMAIN_NAME:~/homecooked-$CIRCLE_SHA1      
      - run: ssh admin@$EC2_DOMAIN_NAME "cd homecooked-$CIRCLE_SHA1 && mix local.hex --force && mix local.rebar --force && mix deps.get && mix deps.compile && MIX_ENV=prod DATABASE_URL=$DATABASE_URL SECRET_KEY_BASE=$SECRET_KEY_BASE mix release;"
        # sudo _build/prod/rel/homecooked/bin/homecooked eval 'Homecooked.Release.migrate' && _build/prod/rel/homecooked/bin/homecooked daemon; cd ../ && sudo rm -rf !('homecooked-$CIRCLE_SHA1') && cd homecooked-$CIRCLE_SHA1;
        # - run: MIX_ENV=prod DATABASE_URL=$DATABASE_URL SECRET_KEY_BASE=$SECRET_KEY_BASE mix release
      
        # 'pkill homecooked && chmod 664 ~/hc_build/prod/rel/homecooked/bin/homecooked && ~/hc_build/prod/rel/homecooked/bin/homecooked start'
      # - aws-s3/copy:
      #     from: _build/prod/rel/homecooked/bin/homecooked
      #     aws-region: AWS_S3_REGION
      #     aws-access-key-id: AWS_ACCESS_KEY_ID
      #     aws-secret-access-key: AWS_SECRET_ACCESS_KEY
      #     to: 's3://circle-ci-images'
      
      # - run: mix ecto.create
      # - run: mix test

# orbs:
#   aws-s3: circleci/aws-s3@1.0.15
